package dev.abhishekagrahari.programming_mess.navigation

import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
/*
@Composable
fun navigationGraph(navController: NavHostController) {
    NavHost(navController = navController, startDestination = "introduction") {

        // Chapter 1: Introduction
        composable("introduction") { IntroductionScreen(navController) }

        // Chapter 2: Basic Syntax and Structure
        composable("basic_syntax") { BasicSyntaxScreen(navController) }

        // Chapter 3: Control Flow
        composable("control_flow") { ControlFlowScreen(navController) }

        // Chapter 4: Functions
        composable("functions") { FunctionsScreen(navController) }

        // Chapter 5: Arrays and Strings
        composable("arrays_strings") { ArraysStringsScreen(navController) }

        // Chapter 6: Pointers and References
        composable("pointers_references") { PointersReferencesScreen(navController) }

        // Chapter 7: Object-Oriented Programming (OOP)
        composable("oop") { OopScreen(navController) }

        // Chapter 8: Operator Overloading
        composable("operator_overloading") { OperatorOverloadingScreen(navController) }

        // Chapter 9: Advanced Data Structures
        composable("advanced_data_structures") { AdvancedDataStructuresScreen(navController) }

        // Chapter 10: File Handling
        composable("file_handling") { FileHandlingScreen(navController) }

        // Chapter 11: Exception Handling
        composable("exception_handling") { ExceptionHandlingScreen(navController) }

        // Chapter 12: Templates
        composable("templates") { TemplatesScreen(navController) }

        // Chapter 13: Advanced Concepts
        composable("advanced_concepts") { AdvancedConceptsScreen(navController) }

        // Chapter 14: Memory Management
        composable("memory_management") { MemoryManagementScreen(navController) }

        // Chapter 15: Debugging and Optimization
        composable("debugging_optimization") { DebuggingOptimizationScreen(navController) }

        // Chapter 16: C++20 Features
        composable("cpp20_features") { Cpp20FeaturesScreen(navController) }

        // Chapter 17: Competitive Programming in C++
        composable("competitive_programming") { CompetitiveProgrammingScreen(navController) }
    }
}
*/